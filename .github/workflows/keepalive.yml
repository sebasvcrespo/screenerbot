name: Keep-Alive Render

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          echo "Pingeando Render..."
          curl -sf https://screenerbot-qqv7.onrender.com && echo " - OK" || echo " - Render no respondió"
